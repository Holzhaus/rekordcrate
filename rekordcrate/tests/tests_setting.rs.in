#[test]
#[allow(non_snake_case)]
fn setting_{name}() {{
    println!("Parsing file: {filepath}");
    let data = include_bytes!("{filepath}").as_slice();
    let (_input, setting) = Setting::parse(data).expect("failed to parse setting file");
    println!("Setting file: {{:#?}}", setting);
}}
